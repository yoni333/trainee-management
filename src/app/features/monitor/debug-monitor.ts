// Debug helper for monitor component
// Add this to monitor.component.ts constructor for debugging

/*
// Debug store state
this.store.select(state => state).subscribe(fullState => {
  console.log('Full app state:', fullState);
});

this.store.select(selectAllTrainees).subscribe(trainees => {
  console.log('All trainees from store:', trainees);
});
*/
